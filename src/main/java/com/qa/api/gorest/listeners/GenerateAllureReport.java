package com.qa.api.gorest.listeners;

public class GenerateAllureReport {

//	Add this dependency by running npm install allure-commandline --save-dev
//	Run your tests and generate test result data (ie, after running it will generate allure-results folder).
//	From the same project directory run, allure generate allure-results --clean -o allure-report in the command prompt
//	On successfull execution it will generate one more folder allure-reportin your directory.
//	Open index.html file in FireFox to show the report.
//	
//	check whether allure is installed or not, in cmd prompt run allur
//	
//	run following cmd in cmd prompt allure serve path_of_allure-report_folder_generated
//	The above command is used to only view the report
//	
//	allure generate path_of_allure-report_folder_generated --clean -o allure-report
//
//	then there are chances that the index.html might not display any report due to browser constraints, so to open this index.html it is always good practice to open it from the command prompt using below command
//
//	allure open path_of_'allure-report'_folder_generated
	
}
